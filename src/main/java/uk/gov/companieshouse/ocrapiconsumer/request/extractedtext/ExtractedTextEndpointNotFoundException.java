package uk.gov.companieshouse.ocrapiconsumer.request.extractedtext;

/**
 * Throws when the extracted text endpoint passed through cannot be resolved.
 */
public class ExtractedTextEndpointNotFoundException extends Exception {
    public ExtractedTextEndpointNotFoundException() {
        super();
    }
}
